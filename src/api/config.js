/**
 * API Configuration
 * Centralized configuration for API endpoints and settings
 */

export const API_CONFIG = {
    BASE_URL: 'https://api.rizurin.my.id',
    TIMEOUT: 10000, // 10 seconds
    ENDPOINTS: {
        HOME: '/index',
    },
};

export default API_CONFIG;
